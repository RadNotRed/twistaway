{
  "name": "motoplanner",
  "version": "0.1.0",
  "private": true,
  "description": "Privacy-first cross-platform motorcycle route planner monorepo.",
  "workspaces": [
    "apps/api",
    "apps/web",
    "packages/shared"
  ],
  "packageManager": "bun@1.3.14",
  "scripts": {
    "build": "bun run --filter @motoplanner/shared build && bun run --filter @motoplanner/api build && bun run --filter @motoplanner/web build",
    "test": "bun run --workspaces --if-present test",
    "dev:api": "bun run --filter @motoplanner/api dev",
    "dev:web": "bun run --filter @motoplanner/web dev"
  },
  "engines": {
    "node": ">=25.0.0"
  }
}
